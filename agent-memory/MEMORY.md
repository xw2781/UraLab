# Memory Index

- [Pointer-capture drag fix](pointer-capture-drag-fix.md) — mousemove-on-document drag/resize loses control on fast drags; fix is setPointerCapture, now codified as L16 in the arcrho-ui-design codex skill
- [ArcRho dataset-types win over ResQ](arcrho-dataset-types-win-over-resq.md) — a sidecar is "calculated" only when dataset_types.json says so; ResQ's Calculated/Formula never decides it (81/82 Prior Qtr vectors are editable inputs); 138 sidecars re-stamped 2026-08-29, Bridge redeploy still pending
- [When a ResQ formula belongs to the type](resq-formula-belongs-to-type.md) — promote an instance-only formula to the ArcRho dataset type when no instance holds data without it; blank+empty is a stub, blank+numbers is a real input (validated over 260 types / 3,962 classes)
- [Instance formula import design](instance-formula-import-design.md) — 2026-08-31 decisions: import translates ResQ instance formulas to in-cell links, ArcRho-linked cells join the propagation chain with auto-recalc (Excel refs evaluated too; a failed Excel read keeps stale values and warns, never blocks), 81/82 stay hardcoded until cross-project links, delete becomes cascade-with-confirm
- [origin_length is NOT the row count](origin-length-is-not-row-count.md) — origin_length/development_length/PeriodLength are months per period (12 = annual), never cell counts; use origin_count / len(origin_labels) / ResQ .OriginCount — a recurring agent mistake, caused the [1:12]-vs-[1:10] formula-link bug fixed 2026-08-31
- [Adding a project-level Engine job](adding-a-project-level-engine-job.md) — a Project Settings save with no reserving class (Dataset Types, Data Processing Rules) becomes a durable Engine job: contract + Engine worker + app_server job service + mutation/read kinds + JS poll loop with the shell progress window; done 2026-09-05 for the rules save (443 s over SMB before); mutation-kind int-0/empty-list gotcha
- [Rules save refreshes affected datasets](rules-save-refreshes-affected-datasets.md) — 2026-09-05: a Data Processing Rules save only counted stale caches and datasets rebuilt on next open; the Engine rules job now regenerates affected engine datasets and walks dependents via the source refresh job's per-class step; failures keep the job successful with a message
- [ResQ stored-length rules](resq-stored-length-rules.md) — the store moves only on a display-length CHANGE or an explicit stored put, never on a data write; docs/reference/resq_stored_and_display_lengths.md is the full case matrix (A1..F2) and tools/resq_stored_length_probe.py re-runs every case
- [Engine in-process calculator](engine-in-process-calculator.md) — 2026-09-05: "dependent refresh reported errors" on a source refresh was quarterly methods in an annual class whose 3-month precedent request files no Engine claimed; the Engine now runs its own jobs' calculations in-process, job messages carry the walk's first real reason, methods_updated counts the right bucket, unreadable loose requests go to engine_requests.log

- [Persisted JSON v4 progress](persisted-json-v4-progress.md) — plan closed as Implemented 2026-09-06 and moved to docs/plans/completed/; only NJ_Annual_Prod_202605_Fake was converted (the rest are re-imported from ResQ), the ultra review and the forced release were dropped on purpose
- [All users run the latest app version](all-users-run-latest-app-version.md) — project assumption from 2026-09-06: no mandatory release markers, no old-client compatibility, no forced-update steps in plans
- [Commit authorship](commit-authorship.md) - commits are authored by xw2781 with no Claude co-author trailer; this clone needed its local git identity set
- [Agent memory lives in the repo](agent-memory-in-repo.md) — memories are tracked in agent-memory/ and reach the harness path through a directory junction created by tools/link_agent_memory.ps1
- [Dev PC and Client PC identity](dev-pc-and-client-pc-identity.md) — run hostname before assuming which box you are on: L-H2MQ6280FVP is the Client PC where E:\ is a mapped drive, NE7SASWPN02 is the Server PC (formerly Dev PC) that holds E:\ArcRho Server and has ResQ installed
- [Client PC as primary workstation](client-pc-primary-workstation.md) — Engine, Bridge, and Gateway already deploy to the server from the Client PC because the share maps to the same E: letter; Orchestrator/Admin/Launcher untested
- [Deploy without asking](deploy-without-asking.md) — after editing a component's bundled sources, run the deploy yourself rather than leaving it as homework; the only thing that stops you is another session's uncommitted work in the tree
- [Remote component deploy](remote-component-deploy.md) — rebuild via server-components/deploy.py so the server-side Build Listener does the work; check the listener heartbeat's Repository before deploying (it hard-resets that clone, possibly yours), one component's failure skips every later one, deploy `--ref <sha>` when the tree holds others' uncommitted work, and a partial "patch did not apply" is the listener's CRLF pipe bug (fixed in source 2026-08-17, listener restart pending)
- [Prefer the built-in edit tool](prefer-built-in-edit-tool.md) — change files with Edit/Write so the VS Code extension can show a diff panel; shell rewrites only for genuine bulk work
- [Bash tool heredoc pitfalls](bash-tool-heredoc-pitfalls.md) — heredoc bodies here choke on backticks and collapse doubled backslashes; write scripts with the Write tool and run them by path; a file-rewriting script must keep the CRLF working-copy endings
- [frontend/build folder layout](frontend-build-folder-layout.md) — runnable scripts at the top level, helpers in subfolders; moving a build script needs its own path anchor fixed, and a bare `runtime/` name collides with a repo-wide gitignore
- [Local release build](arcrho-local-release-build.md) — build_app_from_local_repo.bat releases from this PC by building in place, so the version bump and release notes land in the working tree; codex.cmd is missing here and blocks the build
- [Shared macro library deploy rule](shared-macro-library-deploy.md) — after editing any active macro, publish to E:\ArcRho Server\shared\macros via publish_macro_library.py; it reads the `# Version:` header block, not MACRO_VERSION, so bump both
- [Bridge restart after deploy](bridge-restart-after-deploy.md) — after a deploy only sessions with a live Orchestrator get their Bridge back; a Bridge started by hand from Admin Control stays down, and only that user can start an Orchestrator, in their own server RDP session (2026-09-06); check apps.bridge.auto_create_instance too
- [ResQ custom-average API](resq-custom-average-api.md) — dfm.CustomAverages(i) gives each average row's real type/settings; AverageType 6 = "User Calculation", and Formula is stale junk on rows that are not 6, so never gate on the formula text; decompiled ResQ help lives at E:\XWSpace\ResQ API Doc
- [Benchmark row imports as User Entry](resq-benchmark-row-imports-as-user-entry.md) — 2026-08-31: ResQ User Calculation rows now import as live ArcRho User Entry formula rows, "first user_entry row" no longer means ResQ's User Entry row, and the Ex hi/lo trim was corrected to ResQ's rule in both Python and JS
- [ResQ COM probe](resq-com-probe.md) — ResQ COM works only on the Server PC (py -3.10 with pywin32 + ResQ3Automation.ResQApplication; service account in E:ArcRho Servernfignfig.json); use gencache.EnsureDispatch, late binding silently returns False/empty for DatasetType.Calculated/Formula; dataset TYPE and dataset INSTANCE formulas are separate stores
- [Migration script local run](migration-script-local-run.md) — Client PC engine venv (3.10.11, full packages) runs resq_data_migration.py up to the ResQ connect; real runs need the Server PC, and VS Code Run is wired to that venv
- [Dev UI cache and restart](arcrho-dev-ui-cache-restart.md) — in-app restart can't pick up frontend/ui edits; ARCRHO_UI_VERSION is pinned per supervisor, and ?v= must be bumped in every importer
- [Theme CSS version pins](theme-css-version-pins.md) — tests pin exact ?v= stamps: dark/high_contrast in color_theme.test.mjs, and ui_automation.js across all three shell importers in review_table.test.mjs
- [Launching ArcRho detached](arcrho-launch-electron-detached.md) — use Win32_Process.Create + start_electron.bat; poll app_ui_ready.json (written once); crash logs in %APPDATA%\arcrho-electron\logs
- [Frontend node test suite](frontend-node-test-suite.md) — run with node-portable + glob; 4 tests already fail at HEAD, baseline via git worktree before blaming a change
- [Python test runner](python-test-runner.md) — pytest lives only in the repo-local .pytest-tools (3.14); C:\Program Files\Python310 has fastapi/pandas for frontend unittest runs from frontend/tests; known failures at HEAD incl. the RS cross-producer contract test
- [Macro tests poisoned by test_resq_dfm_v2](macro-tests-poisoned-by-resq-dfm-v2.md) — the ResQ import macro tests pass alone but 24 fail (slowly) when test_resq_dfm_v2.py runs first in the same pytest process; pre-existing at b3a39003, and nothing reaches the live share
- [DFM save propagation profile](dfm-save-propagation-profile.md) — dependent propagation is ~80% of DFM save I/O; offline profiling harness technique and data-engine offload feasibility notes (2026-08-06)
- [ResQ PercentageDevelopedType enum](resq-percentage-developed-enum.md) — 4 codes incl. pdCumDevFactorsAdjusted=3; typelib constants via gencache; GetCapeCodeMethod for full CC objects
- [Project duplication diagnostics](project-duplication-diagnostics.md) — the UI error is redacted by contract; read status JSON history (varying vs fixed stop point) and runtime\logs\project_duplication.log
- [Macro method-notes persistence](macro-method-notes-persistence.md) — method notes cross payload flows only via the transient `method metadata.method notes` carrier; macro→Notes-tab wiring added 2026-08-12, Save persists to sidecar
- [Propagation hold and test isolation](propagation-hold-and-test-isolation.md) — saves preflight a 423 reserving-class hold (45s/180s freshness); service tests that save MUST use tests/dependent_propagation_workspace_stub.py or they enqueue real jobs on E:\ and flake
- [DFM offline recompute repro](dfm-offline-recompute-repro.md) — diagnose propagation-refresh decisions by importing arcrho_api.dfm_contract directly against live method JSON; found the leading-space `= expr` eval bug 2026-08-14
- [Gateway deploy swap lock](gateway-deploy-swap-lock.md) — deploys now stage into a persistent .<App>.slot while live and swap with three retried renames (build_runtime.stage_deploy/swap_deploy); old .new/.old scheme could fail with WinError 5; first deploy per app is a full copy, deltas after warm-up
- [PI path-load SMB cost](pi-path-load-smb-cost.md) — share cost is per operation, not per byte (a whole-file read measured 220ms on one day and 582ms on another, so re-measure before quoting); the reserving-class path load spends 21 round trips off-gateway, and hosted reads measure 4-17x faster
- [Engine calculation gateway transport](engine-calculation-gateway-transport.md) — Phase 2 live 2026-08-16: exchange + whole /arcrho/tri* run + GET /dataset/{id} hosted on the Gateway (DSV length change 13.4 s -> ~1 s); profile breakdown; transport decides on is_network_path(data_path) so tests stay local
- [Adding a hosted workspace read](adding-a-hosted-workspace-read.md) — register the kind in arcrho_workspace_read_contract + service + router, then rebuild the Gateway; hidden imports and the build-time import probe derive from the contract automatically
- [Adding a hosted save kind](adding-a-hosted-save-kind.md) — one SAVE_JOB_KINDS entry plus save_propagation_roots moves a page's save onto the Engine; the Gateway kinds, hidden imports, and plan resolver all derive from that table
- [Propagation status SMB cache lag](propagation-status-smb-cache-lag.md) — a queued propagation makes the client poll status over SMB where Windows' 10s cache hides an already-written terminal status; the Engine walk itself was 1.3s of a 19s save
- [SMB stat metadata alternation](smb-stat-metadata-alternation.md) — os.stat on E: alternates between fresh and ~9s-stale metadata after a server-side write, so a stat-only baseline taken right after a hosted save can miss it
- [Standalone DSV/DFM tabs are legacy](standalone-dsv-dfm-tabs-legacy.md) — the shell's standalone Dataset Viewer and DFM tabs are being dropped; PI windows and Workflow steps are the only live hosts to design for
- [Hung app-server request diagnosis](hung-app-server-request-diagnosis.md) — uvicorn logs an access line only on response, so a route missing from the log may still be running; corroborate with file mtimes and the audit log
- [Build listener request read race](build-listener-request-read-race.md) — deploy.py failing at once with "build request could not be read: Permission denied" is a share write/watch race; one retry succeeds
- [Hosted-save fix needs Engine deploy](hosted-save-fix-needs-engine-deploy.md) — method saves run on the Engine's bundled app_server copy, so an app_server fix is not live until Engine + Gateway are redeployed; grep the deployed canonical copy to verify
- [Bridge worker claim identity](bridge-worker-claim-identity.md) — a queued ResQ import/sync runs under whichever user's per-session Bridge worker claims it; the status file's NTFS owner names the claimant, and a false "ResQ project not found" meant that user's identity could not see the project (fixed 2026-08-26 by using the config service account)
- [Bridge heartbeat false negative](bridge-heartbeat-false-negative.md) — "ArcRho Bridge became unavailable" from the ResQ import macros can be a single-sample SMB false negative; check the statuses folder on E: first, the Bridge usually finished the request
- [server-components folder rename](server-components-folder-rename.md) — data-engine/ became server-components/ on 2026-08-25; the Build Listener was restarted from the new folder the same day, but every venv and build tree went with the old one, so the next deploy rebuilds them all
- [ResQ export order diagnosis](resq-export-order-diagnosis.md) — the Bridge status JSON under requests\RPC bridge\...\statuses records the real write order; replay the walk offline with build_runtime + _reserving_class_edges + _dependency_ordered_rows against the live class; Needs Review after export came from calculated-dataset links being skipped (fixed 2026-08-28)
- [Offline dependent-walk replay](offline-dependent-walk-replay.md) — DANGER: mocking config.load_workspace_paths does not redirect project data, a "scratch" replay wrote to the live NJ HOL class on 2026-08-30; patch the three get_project_*_dir helpers, probe paths first, byte-copy the class first, and prefer the Gateway receipt JSON for reasons
- [Result Selection unchanged-dependent block](result-selection-unchanged-dependent-block.md) — 2026-08-30 fix, committed and deployed (Engine + Gateway): a status-only RS refresh no longer blocks its DFM/calculated dependents; "Downstream refresh failed after … publication" now names the nested reason; the NJ HOL G 41 - BF Paid blank 2026 value is resolved
- [Bulk method restatement hold](bulk-method-restatement-hold.md) — refreshing many methods in one reserving class needs a 423 wait between each, or all but the first are refused
- [Mixed origin-length precedents](mixed-origin-length-precedents.md) — 26 of 82 BF/CC methods in NJ_Annual_Prod_2026 Q2-May Test cannot refresh because a precedent's origin length differs; pre-existing, verify against HEAD before blaming a change; since 2026-09-03 a DFM regenerates an Engine-built precedent at its own period via precedent_cache_service, BF/CC still refuse
- [Percentage developed from DFM factors](percentage-developed-from-dfm-factors.md) — BF and Cape Cod read it from the DFM's selected development factors, never Latest ÷ Ultimate; changed and deployed 2026-08-30; the ResQ import copies ResQ's own PercentageDevelopedValues(i) since the same day (Bridge redeployed)
- [Deploy staleness is mtime-based](deploy-staleness-is-mtime-based.md) — deploy.py --stale compares the deployed EXE mtime against the newest bundled source; committing never makes a component stale, and a "deploy pending" note may already be false — verify by diffing against the deployed arcrho_canonical copy
- [Reserve review source workbook](reserve-review-inputs-workbook.md) — Inputs.xlsx is authoritative, not the Inputs_v2.xlsx beside it; they disagree on the 2025 accounting cutoff (1.0 vs 1.056) and on growth factors by the 5th decimal
- [Growth and cutoff adjustment rules](growth-and-cutoff-adjustment-rules.md) — the workbook Factor block is already compounded; DFM development period n reads vector row [-n] for at most 3 columns; the basis comes from the input triangle; a quarterly class also holds annual-origin methods; the base average row survives only in the sidecar notes
- [Reserve review input vectors script](reserve-review-input-vectors-script.md) — tools/create_reserve_review_input_datasets.py links the four Inputs.xlsx vectors per class, but uses one period basis per class, so annual methods inside a quarterly class are not covered
- [Electron UI screenshot check](electron-ui-screenshot-check.md) — render a mock-up with the bundled electron.exe (env -u ELECTRON_RUN_AS_NODE, app dir + package.json) and Read the PNG; the only local rasterizer on the Client PC; an in-main http server mapping /ui/* lets the mock import and drive a real ES module with synthetic events
- [Unreviewed-dataset import fix](unreviewed-dataset-import-fix.md) — 2026-09-01: the ResQ import now carries calculated and engine-generated datasets the review hides (one rule in catalog._is_unreviewed_dataset shared by review, narrowing, and merge) and compares each Engine-built dataset with ResQ at two decimals, warning in the macro completion box; deployed the same day
- [BS CRA export of Avg. Selections](bs-cra-export-avg-selections.md) — 2026-09-02: Export macro v2.8.0 writes both Avg. Selections grids via SetUser/SetSelected AvgInflation and AvgCaseReserves; names and enum order confirmed against the decompiled manual, look members up by filename under html/
- [pandas read_csv is not round-trip](pandas-read-csv-not-round-trip.md) — the default float parser caps at 17 digit characters incl. leading zeros (near-zero values lose up to ~1e-12 relative); every method service reads source CSVs this way, so DFM and BS share the limit; float_precision="round_trip" is exact

- [ResQ DFM Curves COM API](resq-dfm-curves-com-api.md) — 2026-09-03 probe: the Ratios-tab tail is CustomAverages(i).TailFactor (AverageRatioValues lies there), CurveValues(col,0) is a tail, the tail selection is SelectedTailFactor not SetSelectedEstimates, log-regression fit rules reproduced to 1e-9 and pinned by python-api/tests/fixtures/dfm_curves_resq_c12.json
- [ResQ COM probe: never call members blindly](resq-com-probe-dont-call-blindly.md) — a dir()-driven probe invoked SelectWholeTailCurve/Refresh* and mutated the in-memory method; read only named getters, treat Set/Select/Refresh/Toggle/Reset/Save as actions, confirm in a fresh process
- [Refresh problem diagnosis logs](refresh-problem-diagnosis-logs.md) — a rules save / source refresh shows only its first problem; the full per-class list is in runtime\logs\source_table_refresh.log, the project name in data_processing_rules_jobs.log's start line; "Precedent refresh failed: <DFM>" with the DFM absent from the list was the RS walk blocking an unchanged DFM (fixed 2026-09-06); remaining BF/CC/F 63 failures in Q3-Aug are pre-existing
- [Triangle roll-up valuation anchor](triangle-rollup-valuation-anchor.md) — 2026-09-06: a coarser view of a stored triangle counts development periods back from the Development End Date (ResQ's 8m/20m/32m ages); rollup_triangle needs valuation_months from dataset_service.valuation_months, and a service test with a fake project must patch get_general_settings_path
- [Dataset window two open paths](dataset-window-two-open-paths.md) — the PI open uses the /dataset/cache/load response as the sidecar (only it sends at_display_shape) while the run path reloads the real sidecar; a dev-mode save on the Server PC runs locally, so file mtimes, not gateway.log, prove whether it happened
- [Engine stored lengths are source granularity](engine-stored-lengths-are-source-granularity.md) — an `engine` sidecar's stored_* pair is the project's source-table granularity (1/1 in a monthly project), not the shape of its own CSV; every reader that opens csv_file needs the engine carve-out, which is why B&S kept failing "Net Loss--Paid is not an annual dataset"
- [Stored at must ask the file](stored-at-must-ask-the-file.md) — "is this dataset still empty?" is a question about the dataset's file, not the grid on screen; asking the grid made a paste discard the Stored at period the user had chosen
- [Linked origin is the stored origin](linked-origin-is-the-stored-origin.md) — a cell link can only be entered at the stored origin period, so linked_origin_length/linked_period_length are retired; only linked_development_length is persisted, and a wrong linked shape made a monthly triangle report 6,786 broken links at the yearly view
